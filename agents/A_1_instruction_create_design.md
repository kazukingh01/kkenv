# 前提条件/共通のルール

## NEVER

- この指示書を編集・削除するな
- 「→」は使用禁止
- 文末に「。」は使わない

## IMPORTANT

- 基本的に日本語での応答をお願いする
- 「→」ではなく、「->」で代用する
- 「。」ではなく、「. 」ピリオド＆半角スペース(文末ではなく、文中の場合)を代用する.

# 目的

"./DESIGN.md" を作成し、中身を完成させよ. "DESIGN.md" はアプリケーションの設計書として機能する事を目指す

# 方法

1. "./DESIGN.md" を作成する
2. 以下の「TEMPLATE」の内容をコピーする
3. 「設計書雛形」にある指示に沿って、"./DESIGN.md" を編集する

# 設計書雛形

- ```template``` で囲まれた以下の内容が **TEMPLATE** (雛形)である
- あなたが編集する箇所は <<< 指示内容. 例）XXXX >>> といった形で、"<<<" と ">>>" で囲んだ箇所である。指示内容に従って編集せよ. 例がある場合は、それに沿った形で編集せよ
- それ以外の箇所はそのままコピーし、編集してはならない

```template

# 前提条件/共通のルール

## NEVER

- この設計書を編集・削除するな
- 「→」は使用禁止
- 文末に「。」は使わない

## IMPORTANT

- 基本的に日本語での応答をお願いする
- 「→」ではなく、「->」で代用する
- 「。」ではなく、「. 」ピリオド＆半角スペース(文末ではなく、文中の場合)を代用する.

# Project Structure

## Technology Stack

<<<
既存のプロジェクトがない場合は、何も記載しなくて良い.
既存のプロジェクトがある場合は、関連するミドルウェア（DBやDocker, Webサーバ）や、アプリケーション（フロントエンド、サーバサイド、スクリプト）に使われているプログラミング言語や主要なライブラリについて、既存のファイルなどを網羅的に調べた後、リスト化して

例)
- Rust
- MongoDB
>>>

## Directory Layout

IMPORTANT: ファイル名は記載しない. ディレクトリ構造とその説明のみを記載する.

<<<
既存のプロジェクトがない場合は、何も記載しなくて良い.
既存のプロジェクトがある場合は、以下の例に沿って、ディレクトリのツリー構造と、そのディレクトリの役割について簡潔にコメントを記載せよ

例)
package_name/
├── src/            # Rustソースコード
│   ├── bin/        # 実行可能バイナリ (各取引所データ収集、相関分析など)
│   ├── db/         # MongoDB統合層 (TimeSeries collections)
│   ├── exchanges/  # 取引所API統合 (Bybit, Binance, HyperLiquid)
│   ├── models/     # データモデル (Trade, Candle, Correlation)
│   └── utils/      # ユーティリティ (シンボル管理、ローソク足生成)
├── data/           # 収集データストレージ
│   ├── inverse/    # 逆先物契約データ
│   ├── linear/     # 線形先物契約データ
│   └── spot/       # 現物取引データ
├── logs/           # アプリケーションログ
├── script/         # 運用スクリプト (監視など)
└── target/         # Rustビルド出力

>>>

## Programming guidelines

<<<
以下の点について熟慮し、あなたがコーディングをする事を前提に、あなたが分かりやすいと感じる記述方法で、過不足ないガイドラインをまとめてほしい. 既存のプロジェクトがない場合は、pythonを前提にして良い. 既存のプロジェクトがある場合は、ソースコードを読んだ上でその言語に適用できる形でまとめてほしい.

- 関数や変数の命名は、それが何に類似性があるかを、語順の始まりからそのグループが明確にするような規約であること. 例えば、car_name, parts_name ではなく、name_car, name_parts といった具合である
- 処理が単純で、入れ子にならないようなシンプルなループ構造では、x などの単純な変数でループすること. 例) listwk = [str(x) for x in range(10)]
- コメントは必要最低限で良い. 基本的には関数名や変数名で何をしているかを読み取れるべきである. ただ、def main() の中は、処理のブロック単位で適切にコメントを記述するべきである.
- エラーハンドリングは必要最低限かつ小規模に使うこと. 全てを try catch で囲むような事をしてはならない
- 関数に対する長々としたコメントは基本的に必要ないが、関数名だけでは読み取れず表現しきれない複雑な処理の場合は、簡潔にコメントを記載すること
- ログは "年月日分秒ミリ秒", "ファイル名", "関数名", "ログレベル" の記載をともなうこと
- 外部ソースとのデータの読み書きを伴う場合は、その出力結果をログで簡潔に示すこと. 例）データ件数や、一部データ、dataframe の shape など
- ログは、コメントを代用できるものである. ある処理ブロックの開始時は、コメントの代わりにINFOログで処理の開始を記載して良い
- python に限るが、 DEBUG の使用は避ける（DEBUGを表示しないモードでも、内部的に処理が走り、その内容を表示するための処理で必要以上に重くなるため）
- for ループ内などの単位時間あたりに多くの処理が走るブロックに書くログは、DEBUGで書く
- ある一定以上の処理を伴う関数は、開始と終わりにINFOログを書く. 特別にログに表示したい引数（例えば、処理ロジックを分岐するような mode のような引数）が無ければ logging.info("START") と logging.info("END") だけで良い
- python の場合は、関数宣言の直後に assert で、引数のバリデーションを行う事. それ以外の言語でも、必要な場合はそのバリデーションを行うこと
- 新規に関数を定義する場合、必ず既存の関数を見直して流用できるかどうかを検討すること
- 新規に関数を定義する場合、本当にそのファイルに記載して良いか、Directory Layout を見直した上で検討すること
- 新規にファイルを作成する場合、本当にそのファイルが必要か、既存のファイルに関数を追加するだけで良いのはないか、について、Directory Layout を見直した上で検討すること. ファイル作成しても全体構造のシンプルさや最適化具合に問題が無い場合、新規にファイル作成して良い.
- コード改修は、常に共通化できるロジックを模索し、常に全体のコードや構造が最適化される事を検討すること
- テストコードは書かなくて良い
- データベースへの書き込みロジックを伴う場合は、データベース接続を伴うクラスに必ず dummy 接続を用意する
- データベースへの接続を伴うアプリケーションの場合は、データベースの接続情報は環境変数用のファイルを用意し、そこには dummy 接続を制御するフラグも用意し、デフォルトは dummy 接続を用いるようにする
- あなたがテストやコマンドを実行する場合は、必ず dummy 接続を利用する
- コード量は基本的に少ない方が良いが、処理の複雑さや可読性を考慮した場合、冗長的になっても良い

>>>

# Project Overview

## Purpose & Goals

<<<
既存のプロジェクトがない場合は、何も記載しなくて良い.
既存のプロジェクトがある場合は、あなたが既に調べたソースコードの内容を踏まえて、簡潔に記述せよ.
>>>

```

