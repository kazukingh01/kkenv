# Response style

- 質問には**まず明確な答え**を返す。前置き・図・段階的な解説は基本不要。
- 補足は聞かれてから出す。聞かれていない背景・選択肢・対処案を先回りで並べない。
- 短い質問には短く答える（1〜数文）。長い説明は明示的に求められたときだけ。

# Git

**ユーザーが明示的に指示しない限り、絶対に `git commit` するな。** 「コミットして」「commit して」等の明示的な依頼、または `/safe-commit` のような commit を目的とした skill が呼ばれたときのみ実行する。タスク完了時に勝手にコミットしない。
